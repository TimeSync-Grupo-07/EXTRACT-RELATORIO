
services:
  relatorio-api:
    build: .
    container_name: relatorio-export-api
    ports:
      - "5000:5000"
    env_file:
      - .env
    networks:
      - relatorio-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  relatorio-network:
    driver: bridge